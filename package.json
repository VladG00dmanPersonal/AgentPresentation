{
  "name": "soviet-constructivism-presentation",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node src/build.js",
    "render": "mkdir -p output/pdf && libreoffice --headless --convert-to pdf --outdir output/pdf output/soviet-constructivism.pptx",
    "test": "node --test"
  },
  "dependencies": {
    "image-size": "^1.2.1",
    "pptxgenjs": "^3.12.0"
  },
  "devDependencies": {
    "jszip": "^3.10.1"
  }
}
